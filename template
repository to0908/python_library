from sys import stdin

def solve():
    readline = stdin.readline
    # n = int(input())
    n,m = map(int, readline().split())
    a = list(map(int, readline().split()))
    
    # short coding: int n and size n list    
    n,*a=map(int,open(0).read().split())


def main():
    solve()

main()
